$udo dmidecode |grep -A16 "Memory Device$"
 rsync

 bob@hp:handyScripts$ sudo apt-get install inetutils-traceroute

 /usr/sbin/logrotate /etc/logrotate.conf
 bob@hp:trunk$ cat  /var/lib/logrotate/status 

$ /usr/bin/env  ruby
ulimit -n # /etc/security/limits.conf

使用sudo fdisk -l 命令查看自己的分区
sudo mount -o codepage=936,iocharset=utf8 /dev/sda6 /mnt/localdisk

df
du -sh /* | sort -nr
