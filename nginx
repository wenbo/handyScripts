$ sudo ./sbin/nginx -V

--add-module=/usr/local/rvm/gems/ruby-1.9.3-p125/gems/passenger-3.0.19/ext/nginx
--with-pcre=/home/bob/src/untar/pcre-8.21

"rewrite or internal redirection cycle"

你可以使用这条命令来查看当前的CPU情况：
cat /proc/cpuinfo
或者直接显示逻辑CPU的个数：
cat /proc/cpuinfo |grep "processor"|wc -l
